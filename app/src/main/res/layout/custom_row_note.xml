<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clipToPadding="true"
    app:cardCornerRadius="12dp"
    app:cardUseCompatPadding="true">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/settings_item_rounded_corners_background"
        android:backgroundTint="@color/colorWhite"
        android:padding="24dp">

        <TextView
            android:id="@+id/note_recycler_view_note_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="Notes here"
            android:textColor="@color/colorBlack"
            android:textSize="18sp" />

        <ImageView
            android:id="@+id/note_recycler_view_note_edit_image_view"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_alignParentEnd="true"
            android:layout_marginRight="8dp"
            android:contentDescription="EditNoteButton"
            android:src="@drawable/note_thumbtack" />

        <TextView
            android:id="@+id/note_recycler_view_note_date"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/note_recycler_view_note_title"
            android:layout_marginTop="12dp"
            android:text="Note's date here"
            android:textColor="@color/exo_gray"
            android:textSize="18sp" />
    </RelativeLayout>

</androidx.cardview.widget.CardView>
